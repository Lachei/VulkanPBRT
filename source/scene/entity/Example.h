#pragma once
//TODO demo
//*********initial one Entity manager for all entities
//EntityManager entityManager;
// _id = 0; 
// ***get one inode from assimp
// *** extract from infos from assimp
// ...
// *** create one entity for it and let manager take it over
// CameraEntity& camerai = entityManager->createEntity(_id);
// CameraEntity->AddComponent(some_components_ from_camareNode, for example, camera tranform);
// *** define animation for this entity
//AnimationBehavior cameraAnimationi = new AnimationBehavior(camerai);
// *** define some data to edit the animation
// ...for example frame time
// *** run the animation
// cameraAnimationi.update(frame_time);
